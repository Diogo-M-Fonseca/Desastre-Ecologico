
using UnityEngine;

public class PointUI : MonoBehaviour
{

    private void Update()
    {
        PlayerLogic playerLogic = FindAnyObjectByType<PlayerLogic>();
        if (playerLogic != null)
        {
        
        }
    }
}
